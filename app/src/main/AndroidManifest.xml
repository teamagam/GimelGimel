<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.teamagam.gimelgimel"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <!-- todo: clean -->
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.READ_PROFILE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature android:name="android.hardware.camera"
                  android:required="true" />

    <application
        android:name=".app.GGApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".app.view.LauncherActivity"
            android:label="@string/activity_launcher_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".app.view.MainActivity"
            android:label="@string/activity_main_title"/>
        <activity
            android:name=".app.view.settings.SettingsActivity"
            android:label="@string/activity_settings_title"/>
        <activity
            android:name=".app.view.LoginActivity"
            android:label="@string/title_activity_login"
            android:parentActivityName=".app.view.LauncherActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.teamagam.gimelgimel.app.view.LauncherActivity"/>
        </activity>

        <service
            android:name=".app.network.services.GGService"
            android:exported="false">
        </service>
        <service
            android:name=".app.network.services.GGImageService"
            android:exported="false"/>


        <activity
            android:name=".app.view.FriendsActivity"
            android:label="@string/activity_friends_title"
            android:parentActivityName=".app.view.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.teamagam.gimelgimel.app.view.MainActivity"/>
        </activity>

        <service
            android:name="com.teamagam.gimelgimel.app.network.services.GGMessageLongPollingService"
            android:exported="false">
        </service>

        <receiver android:name=".app.network.receivers.NetworkChangeReceiver" >
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
